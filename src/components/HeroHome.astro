---
import { Image } from 'astro:assets';
import arrow from '../assets/arrow.svg';
import badge from '../assets/badge.svg';
import wave from '../assets/wave.svg';
import wave3 from '../assets/wave-3.svg';
---

<div class="flex items-center py-12 lg:py-20 bg-black border-b border-[#2E2E2E] lg:border-b-0">
    <div class="text-center max-w-7xl mx-auto px-4 md:px-8">
        <h1 class="font-bold leading-tight lg:leading-tight md:leading-tight text-4xl sm:text-6xl md:text-6xl lg:text-[8.7505rem] pb-4 md:pb-12">
            <div class="flex items-center justify-center gap-4 lg:gap-10">
                <Image src={wave3} alt="Wave" class="w-12 h-12 sm:w-16 sm:h-16 md:w-20 md:h-20 lg:w-36 lg:h-36 animate-wave" />
                <span>I'm Maribel,</span>
            </div>
            a <span class="text-indigo-400">Product Designer</span>.
        </h1>
        <p class="font-regular text-lg md:text-xl lg:text-[1.65rem] mb-0 lg:mb-20 max-w-5xl mx-auto text-[#dedde0] lg:leading-normal">I craft user-centric solutions that balance user delight with business impact.  
        </p>
        <div class="relative hidden lg:inline-block">
            <a href="#work" class="cursor-pointer">
                <Image src={badge} alt="Badge" class="w-20 h-20 animate-spin-slow" />
                <Image src={arrow} alt="Scroll down" class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-6 h-6" />
            </a>
        </div>
    </div>
</div>

<style>
    @keyframes spin {
        from {
            transform: rotate(0deg);
        }
        to {
            transform: rotate(360deg);
        }
    }

    @keyframes wave {
        0%, 100% {
            transform: rotate(0deg);
        }
        25%, 75% {
            transform: rotate(-10deg);
        }
        50% {
            transform: rotate(10deg);
        }
    }

    :global(.animate-spin-slow) {
        animation: spin 12s linear infinite;
    }

    :global(.animate-wave) {
        transform-origin: bottom center;
        animation: wave 1s ease-in-out;
    }
</style>

